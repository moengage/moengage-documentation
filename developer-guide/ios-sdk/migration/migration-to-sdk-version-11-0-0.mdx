---
title: "Migration to SDK version 11.0.0"
description: "Migrate your MoEngage iOS SDK to version 11.0.0: initialization enforcement, typed task return values, Sendable requirements, and Swift enum replacements."
keywords: "iOS SDK 11.0.0, migration, typed task, onSuccess, onFailure, Sendable, MoEngageInAppActionType, MoEngageNudgePosition"
---

SDK version 11.0.0 introduces breaking changes to initialization enforcement, API return types, Swift concurrency annotations, and two public Objective-C enums. This page lists the changes you need to make when moving to 11.0.0 or above from 10.x.x. For the complete change list, refer to the [release notes](/release-notes/sdks/ios#22nd-july-2026).

# Initialization Enforcement

Calling a public MoEngage API without having called an initialization method at all now triggers a `fatalError` instead of dropping the call. This applies to Debug, Release, and TestFlight builds alike, and does not depend on whether a debugger is attached. Move every MoEngage API call after `initializeDefaultTestInstance(_:)` or `initializeDefaultLiveInstance(_:)`, which you should call in `application(_:didFinishLaunchingWithOptions:)`.

Calling an initialization method is enough to satisfy the check, whether or not it succeeds. An initialization that runs but fails — for example because of a wrong Workspace ID or a network failure — does not crash the app; downstream API calls fail with the `sdkNotInitialized` reason code through their `onFailure` callback instead.

If you cannot reorder your calls immediately, `MoEngageSDKCore.sharedInstance.disableIntegrationValidator()` suppresses the crash for the current process when called before initialization. Treat it as a stopgap during the upgrade, not a permanent setting.

Refer to [Initialization Before API Calls](/developer-guide/ios-sdk/sdk-integration/basic/sdk-initialization#initialization-before-api-calls) for the cases where the SDK skips this check.

# Typed Task Return Values

Public APIs in the Core, InApp, and Messaging modules that previously returned `Void` now return a task object — a subclass of [`MoEngageBaseTask`](https://moengage.github.io/ios-api-reference/Classes/MoEngageBaseTask.html) such as `MoEngageTrackEventTask` or `MoEngageShowInAppTask`. The task reports whether that specific call succeeded or failed, so conditions such as an uninitialized SDK, a network error, or an invalid Workspace ID reach your code instead of appearing only in the SDK logs.

The return value is annotated `@discardableResult`. Existing calls that ignore it compile and behave as before, and attaching callbacks is optional.

Each API's own page documents the callbacks for that API, with a worked example and the failure codes you are likely to see. Refer to [SDK Initialization](/developer-guide/ios-sdk/sdk-integration/basic/sdk-initialization), [Tracking Events](/developer-guide/ios-sdk/data-tracking/basic/tracking-events), [Tracking User Attributes](/developer-guide/ios-sdk/data-tracking/basic/tracking-user-attributes), [In-App Messages](/developer-guide/ios-sdk/in-app-messages/in-app-nativ), and the [iOS Push Integration Tutorial](/developer-guide/ios-sdk/push/basic/ios-push-integration-tutorial).

## Function References With an Explicit Void Return Type

Assigning one of these methods to a function reference typed `-> Void` no longer compiles. Wrap the call in a closure.

| SDK Version 10.x.x | SDK Version 11.0.0 |
| :-- | :-- |
| `let trackFn: (String, MoEngageProperties?, String?) -> Void = MoEngageSDKAnalytics.sharedInstance.trackEvent` | `let trackFn: (String, MoEngageProperties?, String?) -> Void = { name, properties, appID in`<br />`    _ = MoEngageSDKAnalytics.sharedInstance.trackEvent(name, withProperties: properties, forAppID: appID)`<br />`}` |
| `let initFn: (MoEngageSDKConfig) -> Void = MoEngage.sharedInstance.initializeDefaultLiveInstance` | `let initFn: (MoEngageSDKConfig) -> Void = {`<br />`    _ = MoEngage.sharedInstance.initializeDefaultLiveInstance($0)`<br />`}` |
| `let showFn: () -> Void = MoEngageSDKInApp.sharedInstance.showInApp` | `let showFn: () -> Void = {`<br />`    _ = MoEngageSDKInApp.sharedInstance.showInApp()`<br />`}` |
| `let tokenFn: (Data?) -> Void = MoEngageSDKMessaging.sharedInstance.setPushToken` | `let tokenFn: (Data?) -> Void = {`<br />`    _ = MoEngageSDKMessaging.sharedInstance.setPushToken($0)`<br />`}` |

## Deprecated Completion-Block Variants

The completion-block variants below still work, but they are deprecated in favour of the typed task returned by the same method without the completion block. Migrate to the task and read the outcome through `onSuccess` and `onFailure`.

| Deprecated in 11.0.0 | Use instead |
| :-- | :-- |
| `resetUser(withCompletionBlock:)`, `resetUser(forAppID:withCompletionBlock:)` | `resetUser()` / `resetUser(forAppID:)` returning `MoEngageResetUserTask` |
| `flush(withCompletionBlock:)`, `flush(forAppID:withCompletionBlock:)` | `flush()` / `flush(forAppID:)` returning `MoEngageFlushTask` |
| `getUserIdentities(withCompletionBlock:)` | `getUserIdentities(workspaceId:)` returning `MoEngageGetUserIdentitiesTask` |
| `getSelfHandledInApp(withCompletionBlock:)`, `getSelfHandledInApps(withCompletionBlock:)` | `getSelfHandledInApp()` / `getSelfHandledInApps()` returning the corresponding task |
| `logNotificationReceived(withPayload:withCompletionBlock:)` | `logNotificationReceived(withPayload:)` returning `MoEngageLogNotificationTask` |

For an example of replacing a completion block with the task, refer to [Logout Callback](/developer-guide/ios-sdk/data-tracking/basic/tracking-user-attributes#logout-callback).

# APIs That Return a Task

The following documented APIs return a task you can observe. For the complete list, refer to the [API reference](https://moengage.github.io/ios-api-reference/Classes/MoEngageBaseTask.html).

| API | Class | Task |
| :-- | :-- | :-- |
| `initializeDefaultTestInstance`, `initializeDefaultLiveInstance`, `initializeDefaultInstance` | `MoEngage` | `MoEngageInitializeSDKTask` |
| `trackEvent` | `MoEngageSDKAnalytics` | `MoEngageTrackEventTask` |
| `identifyUser` | `MoEngageSDKAnalytics` | `MoEngageIdentifyUserTask` |
| `getUserIdentities` | `MoEngageSDKAnalytics` | `MoEngageGetUserIdentitiesTask` |
| `resetUser` | `MoEngageSDKAnalytics` | `MoEngageResetUserTask` |
| `setUserAttribute` and the typed variants, including `setName`, `setEmailID`, `setMobileNumber`, `setGender`, `setDateOfBirth`, `setLocation`, `setAlias`, `setUniqueID`, `trackUserPushPreference` | `MoEngageSDKAnalytics` | `MoEngageSetUserAttributeTask` |
| `setDeviceAttribute` | `MoEngageSDKAnalytics` | `MoEngageSetDeviceAttributeTask` |
| `appStatus` | `MoEngageSDKAnalytics` | `MoEngageSetAppStatusTask` |
| `trackLocale` | `MoEngageSDKAnalytics` | `MoEngageTrackDeviceLocaleTask` |
| `enableDataTracking`, `disableDataTracking`, `enableIDFATracking`, `disableIDFATracking`, `enableIDFVTracking`, `disableIDFVTracking` | `MoEngageSDKAnalytics` | `MoEngageTrackingToggleTask` |
| `flush` | `MoEngageSDKAnalytics` | `MoEngageFlushTask` |
| `processURL` | `MoEngageSDKAnalytics` | `MoEngageProcessURLTask` |
| `showInApp` | `MoEngageSDKInApp` | `MoEngageShowInAppTask` |
| `showNudge` | `MoEngageSDKInApp` | `MoEngageShowNudgeTask` |
| `getSelfHandledInApp` | `MoEngageSDKInApp` | `MoEngageGetSelfHandledInAppTask` |
| `getSelfHandledInApps` | `MoEngageSDKInApp` | `MoEngageGetSelfHandledInAppsTask` |
| `selfHandledShown`, `selfHandledClicked`, `selfHandledDismissed` | `MoEngageSDKInApp` | `MoEngageSelfHandledTrackingTask` |
| `setPushToken`, `didFailToRegisterForPush`, `registerForRemoteNotification`, `registerForRemoteProvisionalNotification`, `setUserNotificationCategories` | `MoEngageSDKMessaging` | `MoEngagePushTokenTask` |
| `logNotificationReceived`, `logNotificationClicked` | `MoEngageSDKMessaging` | `MoEngageLogNotificationTask` |
| `navigateToPushSettings` | `MoEngageSDKMessaging` | `MoEngageNavigationTask` |

# Sendable Requirements

Attribute and event property values must now conform to `Sendable`. Under the Swift 6 language mode or strict concurrency checking, call sites that pass non-`Sendable` values need to be updated. Untyped `[String: Any]` dictionaries become `[String: any Sendable]`.

| API | SDK Version 10.x.x | SDK Version 11.0.0 |
| :-- | :-- | :-- |
| `MoEngageSDKAnalytics.setUserAttribute(_:withAttributeName:level:forAppID:)` | `value: Any` | `value: any Sendable` |
| `MoEngageSDKAnalytics.setDeviceAttribute(_:withName:forWorkspaceId:)` | `value: Any` | `value: any Sendable` |
| `MoEngageProperties.init(withAttributes:)` | `attributesDict: [String: Any]` | `attributesDict: [String: any Sendable]` |
| `MoEngageProperties.addAttribute(_:withName:)` | `attrVal: Any` | `attrVal: any Sendable` |

Strings, numbers, booleans, dates, and dictionaries or arrays of these already conform to `Sendable`, so most call sites compile unchanged.

# Main Actor Isolation

## Callback Protocols

`MoEngageAnalyticsCallBack` and `MoEngageAuthenticationError.Listener` are now `Sendable` and `@MainActor` isolated. Conforming types must be safe to use across concurrency domains, and their callback methods are invoked on the main actor. Update implementations that were not already confined to the main thread.

## Cards View Controller APIs

`presentCardsViewController`, `pushCardsViewController`, and `getCardsViewController` are now annotated `@MainActor`. Objective-C callers are unaffected. Swift callers that invoke these methods from a non-main-actor context must hop to the main actor, either by marking the caller `@MainActor` or by wrapping the call in `await MainActor.run { ... }`.

<CodeGroup>

```swift SDK Version 11.0.0 wrap
@MainActor
func openCards() {
    MoEngageSDKCards.sharedInstance.presentCardsViewController()
}
```

```swift Explicit Hop wrap
func openCards() async {
    await MainActor.run {
        MoEngageSDKCards.sharedInstance.presentCardsViewController()
    }
}
```

</CodeGroup>

# Objective-C Enums Migrated to Swift

The public Objective-C enums `MoEngageInAppActionType` and `MoEngageNudgePosition` are now Swift enums. Update your code to the equivalents below. For more information, refer to [In-App Messages](/developer-guide/ios-sdk/in-app-messages/in-app-nativ).

**`MoEngageInAppActionType`**

| | Swift 10.x.x | Swift 11.0.0 | Objective-C 10.x.x | Objective-C 11.0.0 |
| :-- | :-- | :-- | :-- | :-- |
| Navigation action | `NavigationAction` | `.navigationAction` | `NavigationAction` | `MoEngageInAppActionTypeNavigationAction` |
| Custom action | `CustomAction` | `.customAction` | `CustomAction` | `MoEngageInAppActionTypeCustomAction` |

**`MoEngageNudgePosition`**

| | Swift 10.x.x | Swift 11.0.0 | Objective-C 10.x.x | Objective-C 11.0.0 |
| :-- | :-- | :-- | :-- | :-- |
| Top | `NudgePositionTop` | `.top` | `NudgePositionTop` | `MoEngageNudgePositionTop` |
| Bottom | `NudgePositionBottom` | `.bottom` | `NudgePositionBottom` | `MoEngageNudgePositionBottom` |
| Bottom-left | `NudgePositionBottomLeft` | `.bottomLeft` | `NudgePositionBottomLeft` | `MoEngageNudgePositionBottomLeft` |
| Bottom-right | `NudgePositionBottomRight` | `.bottomRight` | `NudgePositionBottomRight` | `MoEngageNudgePositionBottomRight` |
| Any | `NudgePositionAny` | `.any` | `NudgePositionAny` | `MoEngageNudgePositionAny` |
| None | `NudgePositionNone` | `.none` | `NudgePositionNone` | `MoEngageNudgePositionNone` |

# Migration Checklist

- [ ] Every MoEngage API call happens after an initialization method call.
- [ ] Function references typed `-> Void` for the affected APIs are wrapped in closures.
- [ ] Deprecated completion-block variants are replaced with the typed tasks.
- [ ] Attribute and event property values conform to `Sendable`.
- [ ] `MoEngageAnalyticsCallBack` and `MoEngageAuthenticationError.Listener` implementations are safe on the main actor.
- [ ] Swift callers of the Cards view controller APIs run on the main actor.
- [ ] `MoEngageInAppActionType` and `MoEngageNudgePosition` references use the Swift enum cases.
