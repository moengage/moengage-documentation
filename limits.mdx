---
title: "MoEngage Platform Limits"
description: "Reference for rate limits, capacity caps, and configuration limits across MoEngage."
keywords: ["rate limits", "API limits", "platform limits", "quotas", "caps", "flows limits"]
---

import ApiRateLimits from "/snippets/limits/api-rate-limits.mdx";
import ApiPayloadLimits from "/snippets/limits/api-payload-limits.mdx";
import FlowsLimits from "/snippets/limits/flows-limits.mdx";

This page lists the default limits that apply across MoEngage, grouped by area. Values apply per workspace unless the table notes otherwise. Where a limit can be raised, the table shows how.

<Info>
  The values shown are defaults. To request an increase where supported, contact your Customer Success Manager or MoEngage Support.
</Info>

<CardGroup cols={2}>
  <Card title="API Rate Limits" icon="gauge" href="#api-rate-limits" />
  <Card title="API Payload Limits" icon="file-code" href="#api-payload-limits" />
  <Card title="Flows" icon="workflow" href="#flows" />
</CardGroup>

## API Rate Limits

How fast MoEngage APIs can be called. Rates are enforced per workspace, and requests above the limit return `HTTP 429`.

<ApiRateLimits />

## API Payload Limits

Maximum request payload size for the APIs below. Requests above the limit are rejected.

<ApiPayloadLimits />

## Flows

Rules, controls, and capacity limits enforced inside MoEngage Flows.

<FlowsLimits />
