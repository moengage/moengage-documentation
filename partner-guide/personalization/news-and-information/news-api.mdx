---
title: "NewsAPI"
description: "Dynamically pull live headlines and articles into your campaigns at send time using NewsAPI and the MoEngage Content API."
keywords: ["NewsAPI", "Content API", "news aggregation", "headlines personalization", "live articles"]
---

# Introduction

[NewsAPI](https://newsapi.org) is a news aggregation service that indexes articles from 150,000 plus global publishers. It allows developers to search and retrieve headlines, full articles, and sources by keyword, category, country, and language in real time.

# MoEngage × NewsAPI

The MoEngage × NewsAPI integration allows you to dynamically pull live headlines and articles into your campaigns at send time via Content APIs. MoEngage calls the NewsAPI endpoint with a keyword or category relevant to the user or campaign, retrieves current articles, and inserts the data directly into your message across email, push, SMS, in-app, and WhatsApp.

Common use cases include surfacing breaking industry news in B2B email campaigns, personalizing newsletters with headlines matching a user's stated interests, or triggering push notifications when news matching a topic appears.

# Integration

<Info>
**Prerequisites**

Ensure you have a NewsAPI account with an active API key. Keys are free for development use and available at [newsapi.org/register](https://newsapi.org/register). Production use requires a paid plan.
</Info>

## Step 1: Get Your NewsAPI Key

1. Navigate to [newsapi.org/register](https://newsapi.org/register) and create an account.
2. Once registered, your API key is displayed immediately on your account dashboard. Copy it.
3. Review the [NewsAPI pricing page](https://newsapi.org/pricing) to confirm your plan supports the call volume your campaign sends require.

<Note>
**Rate limit**

The free Developer plan allows 100 requests per day and restricts responses to articles published within the last month.
</Note>

## Step 2: Set Up the Content API on MoEngage

1. Navigate to **Dashboard > Settings > APIs** and open the **Content API** tab.
2. Click **Add Content API**.
3. Fill in the details using the table below as a reference.

| Key | Value |
|---|---|
| **Name** | Choose a descriptive name, for example `NewsAPI Top Headlines`. |
| **Method** | GET |
| **API URL** | `https://newsapi.org/v2/top-headlines`. Use `/v2/everything` instead to search all articles by keyword rather than limiting to top headlines. Refer to the [NewsAPI endpoint reference](https://newsapi.org/docs/endpoints) for the full list of available endpoints. |

For **Key Value Parameters**, add the following:

| Parameter | Example value | Notes |
|---|---|---|
| `apiKey` | *Your API key* | Required for every request. Keep this static. |
| `q` | `technology` | Keyword to search for. Can be bound to a user attribute using `@`. |
| `country` | `@user.country` | Two-letter ISO country code. Filters headlines to a specific country. |
| `category` | `business` | Optional. One of: `business`, `entertainment`, `health`, `science`, `sports`, `technology`. |
| `pageSize` | `3` | Number of articles to return. Keep this small (2–5) for campaign use. |

4. Click **Validate** to confirm the platform returns a response, then click **Save**.

Once saved, refer to [Content APIs](/user-guide/campaigns-and-channels/getting-started/campaign-content/content-apis) to learn how to reference response fields inside your campaign templates. For setup details, see [Add a Content API](/user-guide/settings/advanced-settings/add-a-content-api).

## Understanding the API Response

The NewsAPI response contains an `articles` array. Each object in that array represents one article. The fields most commonly referenced in campaign templates are shown below.

| Response field | Template variable example | Description |
|---|---|---|
| `articles[0].title` | `{{ContentApi.articles[0].title}}` | The article headline. |
| `articles[0].description` | `{{ContentApi.articles[0].description}}` | A short summary of the article. |
| `articles[0].url` | `{{ContentApi.articles[0].url}}` | The full URL to the article, use as a CTA link. |
| `articles[0].urlToImage` | `{{ContentApi.articles[0].urlToImage}}` | A thumbnail image URL for the article, usable in email or in-app campaigns. |
| `articles[0].source.name` | `{{ContentApi.articles[0].source.name}}` | The name of the publication, for example "BBC News". |
| `articles[0].publishedAt` | `{{ContentApi.articles[0].publishedAt}}` | The publication timestamp in ISO 8601 format. |
