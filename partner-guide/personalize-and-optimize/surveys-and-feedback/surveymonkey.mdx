---
title: "SurveyMonkey"
---

# Introduction

[SurveyMonkey](https://www.surveymonkey.com/) is a cloud-based survey tool that helps users create, send, and analyze surveys.

# MoEngage × Survey Monkey

SurveyMonkey helps you to drive your business forward by using their online survey and forms tool to capture the voices and opinions of your users. With the MoEngage and SurveyMonkey integration, you can:

- Imports MoEngage users to your SurveyMonkey Contacts.
- Add your MoEngage users to your SurveyMonkey Contact Lists.

## Use Cases

Integrating Survey Monkey with MoEngage helps you with the following use cases:

- **Targeted Feedback Loops** : Instead of blasting surveys to everyone, sync specific MoEngage segments (for example, "High Spenders" or "At-Risk Users") to SurveyMonkey to ask relevant questions.
- **Demographic Synchronization** : Pre-fill survey fields (like Name, City, or Last Purchased Item) by passing MoEngage attributes to SurveyMonkey. This reduces friction for the user and improves survey completion rates.
- **Closed-Loop Marketing** : (Requires Inbound Setup) Use survey responses to tag users in MoEngage. For example, if a user answers "I love Spicy Food" in a survey, update their MoEngage profile to trigger a "Spicy Food Deals" push notification.

# Integration

<Info>
  **Prerequisites**

  - You will need to create a private app to access SurveyMonkey's APIs.
  - Ensure you have your SurveyMonkey's API Key.
</Info>

## Add MoEngage users to SurveyMonkey's Contact Lists

To create a connector campaign on MoEngage:

1. Navigate to Create Campaign \>\> Connectors \>\> Custom and choose the delivery type most suitable to your needs. In this case, select "Event triggered". <img src="/images/partner_20357410766996.png" alt="Screenshot_2022-02-21_at_10.48.46_PM.png" />
2. Select the Trigger criteria and your target audience.
3. On step 2 of the campaign:

### Call the SurveyMonkey API

To forward MoEngage users to SurveyMonkey, use the `POST` method and configure the payload.

- Select the method as POST
- Headers:
  - `Authorization`: `Bearer <your_api_key>`
  - `Content-Type`: `application/json`
- Add the endpoint as Webhook URL: `https://api.surveymonkey.com/v3/contact_lists/{{CONTACT_LIST_ID}}/contacts` - Replace `{{CONTACT_LIST_ID}}` with your contact list's ID.
- Body:
  <CodeGroup>
  ```json JSON
  {
      "first_name": "{{UserAttribute['First Name']}}",
      "last_name": "{{UserAttribute['Last Name']}}",
      "email": "{{UserAttribute['Email (Standard)']}}",
      "custom_fields": {
          "Phone": "{{UserAttribute['Mobile Number (Standard)']}}",
          "City": "{{UserAttribute['Last Known City']}}",
          "Country": "{{UserAttribute['Last Known Country']}}"
      }
  }
  ```
  </CodeGroup>

![](/images/partner_20357410768660.png)

![](/images/partner_45472575730068.png)

- Refer to the [SurveyMonkey API documentation](https://api.surveymonkey.com/v3/docs?shell#api-endpoints-get-contact_lists-contact_list_id-contacts) for more information.

## Preview your request

At this point, your campaign should be ready to test and send. You can also test your connector configuration using the Test functionality available in Step 2 of Create Connector Campaign.<img src="/images/partner_20357410769556.png" alt="Screen_Shot_2022-02-22_at_11.12.42_AM.png" />

You can go ahead and Publish the campaign.