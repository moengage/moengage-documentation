{/* Snippet: MoEngage API payload-size limits (Schema B — capacity caps) */}
{/* Owner: Platform / API PM. Source of truth for customer-visible API payload limits. */}
{/* Do not duplicate these values on any page — import this snippet instead. */}
{/* Payload-size caps are a single value, so they use Schema B, not the rate-limit schema. */}

| Limit | Value | Scope | If exceeded | How to increase |
| --- | --- | --- | --- | --- |
| Catalog API — payload size | 5 MB | Per request | Request is rejected | Not configurable |
| Recommendations API — payload size | 1 MB | Per request | Request is rejected | Not configurable |
| Bulk Import API — payload size | 100 KB | Per request | Request is rejected | Not configurable |
