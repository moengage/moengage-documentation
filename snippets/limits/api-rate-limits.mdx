{/* Snippet: MoEngage API rate limits (Schema A — rate limits, time-windowed) */}
{/* Owner: Platform / API PM. Source of truth for customer-visible API rate limits. */}
{/* Do not duplicate these values on any page — import this snippet instead. */}
{/* Coverage note: only APIs with a confirmed, published rate limit appear here. */}
{/* Engineering to supply gateway/bucket tiers for remaining endpoints before broader publish. */}

| Limit | Per minute | Per hour | Per day | Scope | If exceeded | How to increase |
| --- | --- | --- | --- | --- | --- | --- |
| Campaigns API | 5 | 25 | 100 | Per workspace | Requests return `HTTP 429` | Not configurable |
| Campaign Stats API | 100 | — | — | Per workspace | Requests return `HTTP 429` | Not configurable |
| Catalog API | 100 | 1,000 | — | Per workspace | Requests return `HTTP 429` | Not configurable |
