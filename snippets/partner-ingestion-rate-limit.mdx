To maintain platform stability, MoEngage limits ingestion of {props.partner} data to a maximum of {props.rate || "500 requests per second (RPS)"} per workspace. If your workspace exceeds this limit, MoEngage returns an HTTP `429 (Too Many Requests)` status code.
