If your application handles push token registration itself (that is, the token is not generated by the MoEngage SDK), pass the current push token to the MoEngage SDK again on every fresh login. The SDK clears the previous user's data, including any stored push token, during logout — so your application must supply the token again for the new session.
