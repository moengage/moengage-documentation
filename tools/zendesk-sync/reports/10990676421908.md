# Sync report: Merge User

- **Article**: [10990676421908](https://developers.moengage.com/hc/en-us/articles/10990676421908-Merge-User)
- **YAML**: `api/data/data.yaml`
- **Zendesk last updated**: 2025-11-27T06:04:01Z  (draft=False)
- **OpenAPI operation**: `POST /customer/merge`  (operationId: `None`)

## Summary

- Total diffs: **8**
  - structural / high: 4
  - textual / medium: 3
  - sample / low: 1

## Structural diffs

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "required",
  "sample": "{\"Content-Type\": \"application/json\"}",
  "description": "Set the Content-Type header to application/json for using the Merge User API."
}
```


**OpenAPI**: _(none)_

### `request_body.app_id` — high

_Field `app_id` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "app_id",
  "required": "required",
  "type": "String",
  "description": "This is the Workspace ID of your MoEngage account and needs to be passed along with the request. You can find your MoEngage Workspace ID in Settings -> Account -> APIs -> Workspace ID ."
}
```


**OpenAPI**: _(none)_

### `request_body.merge_data.required` — high

_`merge_data` required flag differs_

**Zendesk**: `required`

**OpenAPI**: `optional`

### `response_fields.error` — high

_Field `error` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "error",
  "type": "JSON Object",
  "description": "This field contains the details of the error:\nmessage - the error message indicating the reason for the failure of the request\ntype - the error type indicating the failure type\nrequest_id - the unique identifier associated with every request"
}
```


**OpenAPI**: _(none)_

## Textual diffs

### `headers.Authorization.description` — medium

_`Authorization` description differs (similarity 0.05)_

**Zendesk**: 
```
This authentication parameter, used for access control, must be passed along with the request. To generate the authentication header, refer to Authentication .
```


**OpenAPI**: 
```
Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.

- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
- **Password**: Use your API Key, which you can find within the **Data** tile.

**Note**: After you generate and save the Data API Key, DO NOT generate a new key unless there is a security breach. After you generate a different Data API key and save it, the authentication will start failing. You must update your existing data tracking. 

For more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials).
```


### `notes[0]` — medium

_Zendesk has a note/callout not reflected in OpenAPI_

**Zendesk**: 
```
Warning
User Merging is a complex functionality and, if misused, can lead to data integrity issues. If the data passed to the API is incorrect, resulting in a merge of two unintended users, MoEngage will not be able to recover/rectify the data. The retained user would have erroneous data, and segmentation queries would not provide the right results.
MoEngage does not support transitive/canonical merging. For example, if user A is merged to B (A ->B) and then user B is merged to C (B ->C), in this scenario, events of user A are not moved to user C.
The Merge User API is not functional in workspaces where the Identity Resolution feature is enabled.
If you are updating the Unique Identifier ( know more ) for a user, use the User Merge API at least 2 hours after you have updated the Unique Identifier.
```


**OpenAPI**: _(none)_

### `response_fields.status.description` — medium

_`status` description differs (similarity 0.39)_

**Zendesk**: 
```
This field contains a brief description (“success” and “fail”) of the request status.
```


**OpenAPI**: 
```
This field contains the status of the request and specifies whether the request was successful. Supported values are : "success", "failure"
```


## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

