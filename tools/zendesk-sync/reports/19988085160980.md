# Sync report: Delete User

- **Article**: [19988085160980](https://developers.moengage.com/hc/en-us/articles/19988085160980-Delete-User)
- **YAML**: `api/data/data.yaml`
- **Zendesk last updated**: 2026-01-28T09:40:09Z  (draft=False)
- **OpenAPI operation**: `POST /customer/delete`  (operationId: `None`)

## Summary

- Total diffs: **13**
  - structural / high: 7
  - textual / medium: 4
  - textual / low: 1
  - sample / low: 1

## Structural diffs

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "",
  "sample": "application/json",
  "description": "Set the Content-Type header to application/json for using the Bulk Update Preferences API."
}
```


**OpenAPI**: _(none)_

### `request_body.app_id` — high

_Field `app_id` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "app_id",
  "required": "required",
  "type": "String",
  "description": "This is your MoEngage account's Workspace ID that has to be passed along with the request.\nYou can find your MoEngage Workspace ID at Settings -> Account -> APIs -> Workspace ID ."
}
```


**OpenAPI**: _(none)_

### `request_body.identity_type.required` — high

_`identity_type` required flag differs_

**Zendesk**: `required`

**OpenAPI**: `optional`

### `request_body.identity_value.required` — high

_`identity_value` required flag differs_

**Zendesk**: `required`

**OpenAPI**: `optional`

### `response_fields.description` — high

_Field `description` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "description",
  "type": "String",
  "description": "This field is present in the response in the case of 429 and 5xx errors and contains the error description."
}
```


**OpenAPI**: _(none)_

### `response_fields.error` — high

_Field `error` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "error",
  "type": "String",
  "description": "This field is present in the response only in the case of errors and contains the error message.\nStructure:\n\"error\": {\n\"message\": \"<Error Message>\",\n\"type\": \"<Error Type>\",\n\"request_id\": \"<Request ID>\"\n}\nmessage - This field contains the error message and describes the reason for the error.\ntype - This field contains the type of error, such as Authentication Required, Invalid Params, and so on.\nrequest_id - This field contains the request-id that identifies the request uniquely."
}
```


**OpenAPI**: _(none)_

### `response_fields.title` — high

_Field `title` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "title",
  "type": "String",
  "description": "This field is present in the response in the case of 429 and 5xx errors and contains the type of error."
}
```


**OpenAPI**: _(none)_

## Textual diffs

### `headers.Authorization.description` — medium

_`Authorization` description differs (similarity 0.05)_

**Zendesk**: 
```
This authentication parameter, used for access control, must be passed along with the request. To generate the authentication header, refer to Authentication .
```


**OpenAPI**: 
```
Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.

- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
- **Password**: Use your API Key, which you can find within the **Data** tile.

**Note**: After you generate and save the Data API Key, DO NOT generate a new key unless there is a security breach. After you generate a different Data API key and save it, the authentication will start failing. You must update your existing data tracking. 

For more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials).
```


### `request_body.identity_type.description` — medium

_`identity_type` description differs (similarity 0.78)_

**Zendesk**: 
```
This field specifies the identifier type. Identifier types can either be customer_id (ID field in the User Profile) or moengage_id (the MoEngageID of the user in the User Profile).
Allowed Values: moengage_id, customer_id
Example : "identity_type": "moengage_id"
```


**OpenAPI**: 
```
This field specifies the identifier type. Identifier types can either be customer_id (ID field in the User Profile) or moengage_id (the MoEngageID of the user in the User Profile). Allowed values are "moengage_id", "customer_id".
```


### `request_body.identity_value.description` — medium

_`identity_value` description differs (similarity 0.71)_

**Zendesk**: 
```
This field specifies the unique identifier that identifies the user.
Example : "identity_value": "6416d1a318r98264512c0f89"
```


**OpenAPI**: `This field specifies the unique identifier that identifies the user.`

### `response_fields.message.description` — medium

_`message` description differs (similarity 0.73)_

**Zendesk**: 
```
This field contains information about whether the response is being processed, the user is deleted, and so on.
Example : "message": "Your request has been accepted and will be processed soon."
```


**OpenAPI**: 
```
This field contains information about whether the response is being processed, the user is deleted, and so on.
```


### `response_fields.status.description` — low

_`status` description differs (similarity 0.88)_

**Zendesk**: 
```
This field contains the status of the request and specifies whether the request was successful.
Supported Values : success, fail
Example : "status": "success"
```


**OpenAPI**: 
```
This field contains the status of the request and specifies whether the request was successful. Supported values are success, fail
```


## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

