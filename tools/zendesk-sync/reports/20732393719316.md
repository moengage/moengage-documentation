# Sync report: Delete Cards API

- **Article**: [20732393719316](https://developers.moengage.com/hc/en-us/articles/20732393719316-Delete-Cards-API)
- **YAML**: `api/cards/cards.yaml`
- **Zendesk last updated**: 2026-02-09T07:06:21Z  (draft=False)
- **OpenAPI operation**: `DELETE /cards/delete`  (operationId: `deleteCards`)

## Summary

- Total diffs: **15**
  - structural / high: 3
  - structural / medium: 4
  - textual / medium: 6
  - sample / low: 2

## Structural diffs

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "required",
  "sample": "{\"Content-Type\": \"application/json\"}",
  "description": "Set the Content-Type header to application/json."
}
```


**OpenAPI**: _(none)_

### `request_body.unique_id or uid` — high

_Field `unique_id or uid` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "unique_id or uid",
  "required": "required (either the unique_id or the uid should be passed in the request)",
  "type": "String",
  "description": "This field is used to identify the cards based on the unique_id or uid specified in the request. The unique_id is the unique value that identifies the user to send the cards. The unique_id is the platform-specific device identifier; refer to Generating unique_id for details .\nThe uid is the unique MoEngage Standard ID (Email ID Standard or the Mobile Number Standard or any unique identifier mapped to a MoEngage Standard attribute) that identifies the user."
}
```


**OpenAPI**: _(none)_

### `response_fields.data.type` — high

_`data` type differs_

**Zendesk**: `Array of JSON Objects`

**OpenAPI**: `object`

### `headers.MOE-APPKEY` — medium

_Field `MOE-APPKEY` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "MOE-APPKEY",
  "required": "required",
  "sample": "",
  "description": "This is the Workspace ID of your MoEngage account that must be passed with the request. You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**."
}
```


### `request_body.uid` — medium

_Field `uid` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "uid",
  "required": "optional",
  "type": "string",
  "description": "The `uid` is the unique **MoEngage Standard ID** (Email ID Standard or Mobile Number Standard) that identifies the user."
}
```


### `request_body.unique_id` — medium

_Field `unique_id` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "unique_id",
  "required": "optional",
  "type": "string",
  "description": "The `unique_id` is the platform-specific device identifier. Refer to [Generating unique_id](/api/cards/fetch-cards-for-user#generating-unique_id) for details."
}
```


### `response_fields.error` — medium

_Field `error` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "error",
  "required": "optional",
  "type": "string",
  "description": "Error message if status is false."
}
```


## Textual diffs

### `headers.Authorization.description` — medium

_`Authorization` description differs (similarity 0.04)_

**Zendesk**: 
```
This authentication parameter, used for access control, must be passed along with the request. To generate the authentication header, refer to Authentication .
```


**OpenAPI**: 
```
Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.

- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
- **Password**: Use your API Key, which you can find within the **Campaign report/Business events/Custom templates/Catalog API/Inform Report** tile.

For more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials).
```


### `rate_limit` — medium

_Rate limit text differs_

**Zendesk**: `The rate limit is 50K RPM and is applicable at the DB(App) level.`

**OpenAPI**: 
```
The rate limit is **50K RPM** (Requests Per Minute) and is applicable at the workspace (App) level.
```


### `request_body.campaign_ids.description` — medium

_`campaign_ids` description differs (similarity 0.70)_

**Zendesk**: 
```
This field denotes the campaign ids associated with the cards being deleted. In the case of a periodic campaign, if the campaign id of the parent campaign is specified, the cards will get deleted for the child campaigns as well.
```


**OpenAPI**: 
```
List of campaign IDs whose cards should be deleted for the user.
In the case of a periodic campaign, if the campaign ID of the parent campaign is specified, the cards will get deleted for the child campaigns as well.
```


### `request_body.platforms.description` — medium

_`platforms` description differs (similarity 0.68)_

**Zendesk**: 
```
This field is used to specify the platforms for which the card should be deleted. If this field is not present in the request, the cards specified for deletion will be deleted from all the platforms they are associated with.
Allowed Values: ANDROID, IOS, WEB
Example:
"platforms": ["ANDROID", "WEB", "IOS"]
```


**OpenAPI**: 
```
Specify platforms to delete cards from. If omitted, the cards specified for deletion will be deleted from all the platforms they are associated with.
Allowed Values: ANDROID, IOS, WEB
```


### `response_fields.data.description` — medium

_`data` description differs (similarity 0.00)_

**Zendesk**: 
```
This field contains the list of cards that satisfy the search criteria.
"data": { "android": {
"success": [<campaign_id>], "failed": [<campaign_id>], "invalid": []
},
"ios": {
"success": [<campaign_id>], "failed": [<campaign_id>], "invalid": []
}
}
The platform-specific response objects – android and ios contain the same structure and have the following information:
"<platform>":{
"success": [<campaign_id>], "failed": [<campaign_id>], "invalid": []
}
success - This field contains the list of campagin ids for which cards have been successfully deleted.
failed - This field contains the list of campaigns ids for which the cards could not be deleted.
invalid - This field contains the list of campaign ids for which cards do not exist for the specified platform.
```


**OpenAPI**: ``

### `response_fields.request_id.description` — medium

_`request_id` description differs (similarity 0.76)_

**Zendesk**: `This field contains the unique identifier that denotes the request.`

**OpenAPI**: `Unique identifier that denotes the request.`

## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

### `samples.responses` — low

_Zendesk has sample responses not in OpenAPI: ['200']_

**Zendesk**: 
```json
[
  "200"
]
```


**OpenAPI**: 
```json
[]
```


