# Sync report: Track Device

- **Article**: [31285296671252](https://developers.moengage.com/hc/en-us/articles/31285296671252-Track-Device)
- **YAML**: `api/data/data.yaml`
- **Zendesk last updated**: 2025-11-27T06:56:40Z  (draft=False)
- **OpenAPI operation**: `POST /device/{app_id}`  (operationId: `None`)

## Summary

- Total diffs: **13**
  - structural / high: 3
  - structural / medium: 2
  - textual / medium: 4
  - textual / low: 3
  - sample / low: 1

## Structural diffs

### `endpoint.url` — high

_Endpoint URL differs_

**Zendesk**: `https://api-{{0X}}.moengage.com/v1/device/{{appId}}`

**OpenAPI**: `https://api-{dc}.moengage.com/v1/device/{app_id}`

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "required",
  "sample": "{\"Content-Type\": \"application/json\"}",
  "description": "Set the Content-Type header to application/json."
}
```


**OpenAPI**: _(none)_

### `request_body.attributes.required` — high

_`attributes` required flag differs_

**Zendesk**: `-`

**OpenAPI**: `required`

### `response_fields.message` — medium

_Field `message` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "message",
  "required": "optional",
  "type": "string",
  "description": ""
}
```


### `response_fields.status` — medium

_Field `status` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "status",
  "required": "optional",
  "type": "string",
  "description": "This field contains the status of the request and specifies whether the request was successful."
}
```


## Textual diffs

### `headers.Authorization.description` — medium

_`Authorization` description differs (similarity 0.10)_

**Zendesk**: 
```
This authentication parameter, used for access control, must be included in the request. For more information on generating the authentication header, refer to Authentication .
```


**OpenAPI**: 
```
Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.

- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
- **Password**: Use your API Key, which you can find within the **Data** tile.

**Note**: After you generate and save the Data API Key, DO NOT generate a new key unless there is a security breach. After you generate a different Data API key and save it, the authentication will start failing. You must update your existing data tracking. 

For more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials).
```


### `notes[0]` — medium

_Zendesk has a note/callout not reflected in OpenAPI_

**Zendesk**: 
```
Information
The appId for your MoEngage account is available on the MoEngage dashboard. We've revamped our Settings UI; you can find the app_id using the following path: Settings > Account > APIs > Workspace ID
```


**OpenAPI**: _(none)_

### `rate_limit` — medium

_Rate limit text differs_

**Zendesk**: `You can track 10,000 devices per minute.`

**OpenAPI**: 
```
A single API request contains one or more device updates. Maintain a rate limit of 10,000 device updates per minute..
```


### `request_body.attributes.description` — medium

_`attributes` description differs (similarity 0.76)_

**Zendesk**: 
```
This field contains the device attributes to add or update in the device profile. For more information, refer to Device Attributes.
```


**OpenAPI**: 
```
This field contains the device attributes to add or update in the device profile.
```


### `headers.X-Forwarded-For.description` — low

_`X-Forwarded-For` description differs (similarity 0.92)_

**Zendesk**: 
```
The 'X-Forwarded-For' header is used to specify the IP address of the client that made the request. Proxy servers or load balancers may add this header. The header value must contain the IP a ddress of the original client that initiated the request. Multiple IP addresses may be specified in the header value, separated by commas.
```


**OpenAPI**: 
```
The 'X-Forwarded-For' header is used to specify the IP address of the client that made the request. This header may be added by proxy servers or load balancers. The header value must contain the IP address of the original client that initiated the request. Multiple IP addresses may be specified in the header value, separated by commas.
```


### `request_body.device_id.description` — low

_`device_id` description differs (similarity 0.95)_

**Zendesk**: 
```
This field denotes the unique identifier used to identify the device associated with a user in MoEngage.
If your app is integrated with MoEngage SDK, the SDK assigns a unique device_id to the devices of your end-users.
If your app is not integrated with MoEngage SDK, you can use unique alphanumeric values or other device attributes such as Push token, GAID/IDFV to assign unique device_id values to your users' devices.
```


**OpenAPI**: 
```
This field denotes the unique identifier used to identify the device associated with a user in MoEngage.
If your app is integrated with MoEngage SDK, the SDK assigns a unique device_id to the devices of your end-users.
If your app is not integrated with MoEngage SDK, you can use unique alphanumeric values or other device attributes such as Push token, GAID/IDFV to assign unique device_id values to devices of your user.
```


### `request_body.type.description` — low

_`type` description differs (similarity 0.91)_

**Zendesk**: 
```
This field denotes the type of request. Supported value is: device
Example: "type": "device"
Note : This field is case-sensitive. When passing the value in the request, follow the case as in the example.
```


**OpenAPI**: 
```
This field denotes the type of request. Supported value is: device. This field is case-sensitive. When passing the value in the request, follow the case as in the example.
```


## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

