# Sync report: Stats API

- **Article**: [32895300259732](https://developers.moengage.com/hc/en-us/articles/32895300259732-Stats-API)
- **YAML**: `api/stats-report/stats-report.yaml`
- **Zendesk last updated**: 2026-02-10T03:57:53Z  (draft=False)
- **OpenAPI operation**: `POST /core-services/v1/campaign-stats`  (operationId: `getCampaignStats`)

## Summary

- Total diffs: **15**
  - structural / high: 1
  - structural / medium: 7
  - textual / medium: 1
  - textual / low: 4
  - sample / low: 2

## Structural diffs

### `endpoint.url` — high

_Endpoint URL differs_

**Zendesk**: `https://api-{{0X}}.moengage.com/core-services/v1/campaign-stats`

**OpenAPI**: `https://api-{dc}.moengage.com/core-services/v1/campaign-stats`

### `headers.Authorization` — medium

_Field `Authorization` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "Authorization",
  "required": "required",
  "sample": "",
  "description": "Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.\n\n- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.\n- **Password**: Use your API Key, which you can find within the **Campaign report/Business events/Custom templates/Catalog API/Inform Report** tile.\n\nFor more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials)."
}
```


### `headers.MOE-APPKEY` — medium

_Field `MOE-APPKEY` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "MOE-APPKEY",
  "required": "required",
  "sample": "YOUR_WORKSPACE_ID_XXXX",
  "description": "This is the Workspace ID of your MoEngage account that must be passed with the request. You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**."
}
```


### `response_fields.current_page` — medium

_Field `current_page` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "current_page",
  "required": "optional",
  "type": "integer",
  "description": "This field indicates the current page number."
}
```


### `response_fields.data` — medium

_Field `data` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "data",
  "required": "optional",
  "type": "object",
  "description": "This object contains the campaign stats. Keys are Campaign IDs."
}
```


### `response_fields.response_id` — medium

_Field `response_id` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "response_id",
  "required": "optional",
  "type": "string",
  "description": "The response ID, which is the same as the request ID passed in the payload."
}
```


### `response_fields.total_campaigns` — medium

_Field `total_campaigns` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "total_campaigns",
  "required": "optional",
  "type": "integer",
  "description": "This field indicates the total number of campaigns that are requested in the payload."
}
```


### `response_fields.total_pages` — medium

_Field `total_pages` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "total_pages",
  "required": "optional",
  "type": "integer",
  "description": "This field indicates the total page. It is equal to the total campaigns, which is limited to 10 for each call and 10 to be displayed for each page."
}
```


## Textual diffs

### `rate_limit` — medium

_Rate limit text differs_

**Zendesk**: 
```
This API provides all version stats, and the call rate is limited to 100 API calls per minute or workspace.
```


**OpenAPI**: 
```
This API provides all version stats, and the call rate is limited to **100 API calls per minute** per workspace.
```


### `response_codes.200.description` — low

_Response code 200 description differs (similarity 0.39)_

**Zendesk**: `This response is returned upon a successful request.`

**OpenAPI**: `Successful response.`

### `response_codes.403.description` — low

_Response code 403 description differs (similarity 0.77)_

**Zendesk**: 
```
This response is returned when our account does not have access to the campaign stats management features. For further assistance, contact the MoEngage team.
```


**OpenAPI**: 
```
**Forbidden**
This response is returned when our account does not have access to the campaign stats management features.
```


### `response_codes.429.description` — low

_Response code 429 description differs (similarity 0.89)_

**Zendesk**: 
```
This response is returned when the number of requests per minute has exceeded the rate limit.
```


**OpenAPI**: 
```
**Too many requests**
This response is returned when the number of requests per minute has exceeded the rate limit.
```


### `response_codes.5xx.description` — low

_Response code 5xx description differs (similarity 0.65)_

**Zendesk**: 
```
This response is returned when the system runs into an unexpected error. For further assistance, contact the MoEngage team.
```


**OpenAPI**: 
```
**Internal server error**
This response is returned when the system runs into an unexpected error.
```


## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

### `samples.responses` — low

_Zendesk has sample responses not in OpenAPI: ['429']_

**Zendesk**: 
```json
[
  "429"
]
```


**OpenAPI**: 
```json
[]
```


