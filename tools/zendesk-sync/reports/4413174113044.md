# Sync report: Bulk Import

- **Article**: [4413174113044](https://developers.moengage.com/hc/en-us/articles/4413174113044-Bulk-Import)
- **YAML**: `api/data/data.yaml`
- **Zendesk last updated**: 2025-11-27T09:37:29Z  (draft=False)
- **OpenAPI operation**: `POST /transition/{Workspace_ID}`  (operationId: `None`)

## Summary

- Total diffs: **17**
  - structural / high: 4
  - structural / medium: 2
  - structural / low: 2
  - textual / medium: 6
  - textual / low: 1
  - sample / low: 2

## Structural diffs

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "required",
  "sample": "{\"Content-Type\": \"application/json\"}",
  "description": "Set the Content-Type header to application/json."
}
```


**OpenAPI**: _(none)_

### `headers.X-Forwarded-For.required` — high

_`X-Forwarded-For` required flag differs_

**Zendesk**: `required`

**OpenAPI**: `optional`

### `request_body.app_id` — high

_Field `app_id` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "app_id",
  "required": "required",
  "type": "String",
  "description": "This is your MoEngage account's Workspace ID that has to be passed along with the request. You can find your MoEngage Workspace ID at Settings > Account > APIs > Workspace ID (earlier app id) ."
}
```


**OpenAPI**: _(none)_

### `request_body.elements.type` — high

_`elements` type differs_

**Zendesk**: `List`

**OpenAPI**: `array`

### `response_fields.message` — medium

_Field `message` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "message",
  "required": "optional",
  "type": "string",
  "description": ""
}
```


### `response_fields.status` — medium

_Field `status` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "status",
  "required": "optional",
  "type": "string",
  "description": "This field contains the status of the request and specifies whether the request was successful."
}
```


### `response_codes.200` — low

_Response code `200` in OpenAPI only_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "code": "200",
  "state": "",
  "description": "This response is returned when the request is processed successfully."
}
```


### `response_codes.400` — low

_Response code `400` in OpenAPI only_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "code": "400",
  "state": "",
  "description": "This response is returned when the required parameters are missing from the request, attributes are not found, and incorrect values are passed."
}
```


## Textual diffs

### `headers.Authorization.description` — medium

_`Authorization` description differs (similarity 0.05)_

**Zendesk**: 
```
This authentication parameter, used for access control, must be passed along with the request. To generate the authentication header, refer to Authentication .
```


**OpenAPI**: 
```
Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.

- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
- **Password**: Use your API Key, which you can find within the **Data** tile.

**Note**: After you generate and save the Data API Key, DO NOT generate a new key unless there is a security breach. After you generate a different Data API key and save it, the authentication will start failing. You must update your existing data tracking. 

For more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials).
```


### `notes[0]` — medium

_Zendesk has a note/callout not reflected in OpenAPI_

**Zendesk**: 
```
Note
The Workspace_ID for your MoEngage account is available at Settings -> Account -> APIs -> Workspace ID (earlier app id) .
If you have Portfolio enabled for your workspace, you need to pass project_code in the API endpoint. This identifies which project a user or event belongs to. For more information, refer to Portfolio: Data Ingestion and Management .
```


**OpenAPI**: _(none)_

### `notes[1]` — medium

_Zendesk has a note/callout not reflected in OpenAPI_

**Zendesk**: `Note`

**OpenAPI**: _(none)_

### `rate_limit` — medium

_Rate limit text differs_

**Zendesk**: ``

**OpenAPI**: 
```
A single bulk import API contains users, devices, and events together. Send a maximum of 10,000 users and 30,000 events per minute across all API requests.
```


### `request_body.elements.description` — medium

_`elements` description differs (similarity 0.75)_

**Zendesk**: 
```
List of data points (events and customers) to track.
Note : The type field in this List denotes whether the information is for a customer or an event. This field is case-sensitive. Follow the case as in the example when passing the value in the request.
Allowed Values: customer, event, device
Examples: "type": "customer" "type": "event" "type": "device"
"type": "customer"
"type": "event"
"type": "device"
```


**OpenAPI**: 
```
List of data points (events and customers) to track. The type field in this list denotes whether the information is for a customer or an event. This field is case-sensitive. Follow the case as in the example when passing the value in the request.
```


### `request_body.type.description` — medium

_`type` description differs (similarity 0.83)_

**Zendesk**: 
```
This is used to identify the type of request.
Allowed Value: transition
Example: "type": "transition"
Note: This field is case-sensitive. Follow the case as in the example when passing the value in the request.
```


**OpenAPI**: 
```
This is used to identify the type of request. This field is case-sensitive. Follow the case as in the example when passing the value in the request.
```


### `notes.openapi[0]` — low

_OpenAPI has a note/callout not present in Zendesk_

**Zendesk**: _(none)_

**OpenAPI**: 
```
All bulk API requests return a 200 response code. Debugging should be done on the user profile on the dashboard.
```


## Sample diffs

### `samples.curl` — low

_cURL sample present on one side only_

**Zendesk**: `present`

**OpenAPI**: `missing`

### `samples.responses` — low

_OpenAPI has sample responses not in Zendesk: ['200', '400']_

**Zendesk**: 
```json
[]
```


**OpenAPI**: 
```json
[
  "200",
  "400"
]
```


