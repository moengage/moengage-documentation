# Sync report: Search content block

- **Article**: [9611848319508](https://developers.moengage.com/hc/en-us/articles/9611848319508-Search-content-block)
- **YAML**: `api/content-blocks/content-blocks.yaml`
- **Zendesk last updated**: 2026-01-29T07:28:05Z  (draft=False)
- **OpenAPI operation**: `POST /content-blocks/search`  (operationId: `None`)

## Summary

- Total diffs: **10**
  - structural / high: 1
  - structural / medium: 3
  - structural / low: 3
  - textual / medium: 2
  - sample / low: 1

## Structural diffs

### `headers.Content-Type` — high

_Field `Content-Type` exists in Zendesk but is missing from OpenAPI_

**Zendesk**: 
```json
{
  "name": "Content-Type",
  "required": "",
  "sample": "",
  "description": "--"
}
```


**OpenAPI**: _(none)_

### `headers.Authorization` — medium

_Field `Authorization` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "Authorization",
  "required": "required",
  "sample": "",
  "description": "Authentication is done via Basic Auth. This requires a base64-encoded string of your credentials in the format 'username:password'.\n\n- **Username**: Use your MoEngage workspace ID (also known as the App ID). You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.\n- **Password**: Use your API Key, which you can find within the **Campaign report/Business events/Custom templates/Catalog API/Inform Report** tile.\n\nFor more information on authentication and getting your credentials, refer [here](https://www.moengage.com/docs/api/introduction#getting-your-credentials)."
}
```


### `request_body.filters` — medium

_Field `filters` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "filters",
  "required": "optional",
  "type": "object",
  "description": "A set of filters to apply to the search.\n<Note>You can send empty filters if you want all the content blocks: `{ \"filters\" : {} }`</Note>"
}
```


### `response_fields.data` — medium

_Field `data` exists in OpenAPI but not in Zendesk (may be deprecated or Zendesk is missing it)_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "name": "data",
  "required": "optional",
  "type": "array of object",
  "description": ""
}
```


### `response_codes.200` — low

_Response code `200` in OpenAPI only_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "code": "200",
  "state": "",
  "description": "Success"
}
```


### `response_codes.400` — low

_Response code `400` in OpenAPI only_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "code": "400",
  "state": "",
  "description": "Bad Request"
}
```


### `response_codes.5xx` — low

_Response code `5xx` in OpenAPI only_

**Zendesk**: _(none)_

**OpenAPI**: 
```json
{
  "code": "5XX",
  "state": "",
  "description": "Failure. Server issue"
}
```


## Textual diffs

### `headers.MOE-APPKEY.description` — medium

_`MOE-APPKEY` description differs (similarity 0.42)_

**Zendesk**: 
```
You can find your MoEngage Workspace key at Settings -> Account -> APIs -> Workspace ID .
```


**OpenAPI**: 
```
This is the Workspace ID of your MoEngage account that must be passed with the request. You can find it in the MoEngage dashboard at **Settings** > **Account** > **APIs** > **Workspace ID (earlier app id)**.
```


### `notes[0]` — medium

_Zendesk has a note/callout not reflected in OpenAPI_

**Zendesk**: 
```
Note
You can send empty filters if you want all the content blocks.
```


**OpenAPI**: _(none)_

## Sample diffs

### `samples.responses` — low

_OpenAPI has sample responses not in Zendesk: ['200', '400', '5xx']_

**Zendesk**: 
```json
[]
```


**OpenAPI**: 
```json
[
  "200",
  "400",
  "5xx"
]
```


