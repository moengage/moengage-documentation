---
title: "Typography Specimen"
description: "Preview-only page for reviewing the heading scale. Delete before merging."
noindex: true
---

<Note>
  Preview-only page for typography review. Delete before merging to `main`.
</Note>

The page title above is Level 0. Every heading level below appears with a
paragraph of body copy, so each step can be judged against the 16px body text it
has to introduce.

# Level 1 Heading

Body copy at 16px. Segments group users by their attributes, behavior, and
affinity, so you can target a campaign at the people most likely to act on it.
Use this paragraph to judge how far the heading above sits from running text.

## Level 2 Heading

Body copy at 16px. A flow orchestrates a sequence of messages across channels
and waits for a user to reach each step before sending the next one.

### Level 3 Heading

Body copy at 16px. Connectors move event and user data between MoEngage and your
warehouse on a schedule you control.

#### Level 4 Heading

Body copy at 16px. Content blocks hold reusable markup that several campaigns can
reference, so a change propagates everywhere at once.

##### Level 5 Heading

Body copy at 16px. Computed traits derive a value from a user's event history,
such as their total spend over the last 30 days.

###### Level 6 Heading

Body copy at 16px. This is the smallest level in the scale, and the point where
size alone stops separating a heading from the paragraph under it.

## Levels In Sequence

The block below stacks the levels with no body copy between them, which is how
the steps in the scale read against each other.

### Level 3 After Level 2

#### Level 4 After Level 3

##### Level 5 After Level 4

###### Level 6 After Level 5

## Headings With Real Page Furniture

Mixing headings with the components they normally sit beside shows whether the
scale still reads once tables, callouts, and code sit between the levels.

### Table Under A Level 3

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | string | Unique identifier for the user. |
| `event_name` | string | Name of the tracked event. |
| `timestamp` | integer | Unix epoch in seconds. |

#### Code Under A Level 4

```json Sample Payload
{
  "user_id": "8f14e45f",
  "event_name": "Product Viewed",
  "timestamp": 1756339200
}
```

##### Callout Under A Level 5

<Tip>
  Body copy inside a component keeps the same 16px size as running text.
</Tip>

## Weight And Case

Headings render in Circular. Confirm that bold headings use a real cut rather
than a browser-synthesized one, and that title case holds at every level.

### Mixed Case At Level 3 With A Longer Line That Wraps Onto A Second Line

#### Mixed Case At Level 4 With A Longer Line That Also Wraps Onto A Second Line To Show Leading
