---
title: "Facebook Audience Settings on MoEngage dashboard"
description: "Complete the one-time setup to connect your Facebook account with MoEngage. Configure App ID, secret, and ad account for custom audience campaigns."
---

This article will help you with the one-time configuration required for Facebook. This configuration establishes a connection between MoEngage and your Facebook account. Before you proceed with the settings, make sure you have the App & Ad account ready as per the required configurations explained in the document [Facebook setup for custom audience integration](/user-guide/campaigns-and-channels/facebook-audience/getting-started-with-facebook-audience/understanding-facebook-setup-for-custom-audience-integration).

<Info>
  - This is a one-time setup; you will only be required to do this step again if the app secret or user token becomes invalid.
  - The FB settings can be added/modified by a user with Admin access to the MoEngage dashboard. For more information, refer to [access roles.](/user-guide/settings/account/team-management/access-roles)
</Info>

Once you are all set, fetch the following fields as explained below.

To integrate your Facebook account with MoEngage, navigate to **Settings** \> **App marketplace** and choose **Facebook audience**.

![FB Audience.png](/images/moengage_dcf843.png)

1. **App ID:**

- Go to [developers.facebook.com/apps](https://developers.facebook.com/apps/). Select the app you want to sync. The **App ID** appears in the top bar.
- Alternatively, in **Meta Business Suite**, go to **Settings** \> **Accounts** \> **Apps**, select the app you want to sync, and copy the **App ID** shown below the app name. {/* VERIFY (live UI): confirm the current Business Suite path to Apps and that the App ID still appears below the app name. */}

2. **App secret:**

- In your [Meta app dashboard](https://developers.facebook.com/apps), select your app, and then go to **App settings** \> **Basic**. {/* VERIFY (live UI): confirm the current label/path for the Basic settings page where App Secret lives. */}
- Next to **App secret**, click **Show**.
- If prompted, enter your password to reveal the **App secret**.

3. **Access token:**

- Go to your [Meta app dashboard](https://developers.facebook.com/apps) and select the app you want to sync with MoEngage.
- Generate an access token that includes the **`ads_management`** and **`ads_read`** permissions. {/* VERIFY (live UI): the old "Marketing API > Tools > Get Access Token" path predates the use-case dashboard. Confirm the current in-dashboard token tool path and label, or whether customers should use the Graph API Explorer / Marketing API authentication flow. */}
- Alternatively, obtain an access token from Meta's [Graph API Explorer](https://developers.facebook.com/tools/explorer/) or the [Marketing API authentication flow](https://developers.facebook.com/docs/marketing-api/overview/authorization/), selecting the `ads_management` and `ads_read` permissions.

{/* TODO(screenshot): Recapture the Meta access-token screen — the existing image shows the old Meta UI. */}
![mceclip1.png](/images/moengage_0ddc25.png)

4. **Business ID** (Optional)**:**

This field is required only if the user whose access token is used here has access to multiple Meta business portfolios and wants to sync with only one particular portfolio. To retrieve your **business portfolio ID**, go to **Meta Business Suite** \> **Settings** \> **Business info**. {/* VERIFY (live UI): confirm the current path to the business portfolio ID (formerly the "Business ID" on business.facebook.com/settings/info). */}

{/* TODO(screenshot): Recapture the Meta business info / portfolio ID screen — the existing image shows the old Meta UI. */}
![mceclip2.png](/images/moengage_041702.png)

After entering all the fields, click the Connect to Facebook button.

On successful connection, we will fetch the list of all Ad Accounts associated with the logged-in user. You can select one as default.

![Screenshot\_2021-01-28\_at\_6.37.50\_PM.png](/images/moengage_d0c1ec.png)

If you wish to change these settings later because the logged-in user no longer has access to your FB business account or for any other reason, click Disconnect Facebook. Add the fields again and click Connect to FB.

<Info>
  - We need these fields to fetch all the Ad accounts from your account. We would also need the access token every time when the campaign will run where we would need to make an API call to the FB.
  - The access token you have entered above is a short-lived access token. It is valid only for a few hours. Once you provide this access token to us, we will convert it into a long-lived access token and store it on our end and we will also extend the token at our end.
  - The long-lived access token has a longer expiration time, such as 60 days or Never. We will periodically check for token validity, and if the token is about to expire within the next 7 days, we will extend the token validity. To retrieve an extended token, app ID & app secret are required.
  - If the user token or app secret is no longer valid, then we will notify you to update the fields on the settings page.
  - In case the access token is not mapped to any business portfolio, the default ad account associated with the user will be fetched and result in a Permissions Error (Business account needed to create/edit this audience). Ensure that your access token is mapped to the appropriate business portfolio in Meta Business Suite settings.

  Once you are connected to Facebook, you can get started with FB Audience campaign creation, as explained in [this article](/user-guide/campaigns-and-channels/facebook-audience/create/create-a-facebook-audience-campaign).
</Info>