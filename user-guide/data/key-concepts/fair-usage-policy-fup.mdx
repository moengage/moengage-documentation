---
title: "Fair Usage Policy (FUP)"
description: "MoEngage's Fair Usage Policy ensures equitable resource access for all customers and protects workspaces from unregulated data ingestion."
---

# Overview

A Fair Usage Policy (FUP) is a set of guidelines and rules implemented to ensure all customers have equal and non-discriminatory access to MoEngage resources.

It helps in the following ways:

- Secures your workspace from unregulated external data tracking, which might increase your user count and overall integrity of data tracking.
- Ensures the fair allocation of MoEngage resources among all its customers. It ensures that large data ingestion caused by improper integration from a limited number of customers does not negatively impact data tracking for other customers.

MoEngage proactively identifies instances of FUP violations and blocks end-user level data tracking. As a marketer, you can still find these users in your workspace and send campaigns to them.

# How Does the Fair Usage Policy Work?

MoEngage has configured the data request limit above which the FUP is violated by analyzing the recent trends in the data tracked using the SDK, and hence, these FUP limits are rarely breached.

Whenever an end-user breaches the maximum number of data requests allowed, MoEngage temporarily blocks the data tracking of the user for a specific time limit. The duration of the temporary block depends on the time interval and the number of data requests received in that interval. However, despite the temporary block, if a user continues to breach the FUP limit two more times consecutively, the data tracking of that user is blocked for 7 days. For more information, refer to [Fair Usage Policy Limits and Actions](/user-guide/data/key-concepts/fair-usage-policy-fup#fair-usage-policy-limits-and-actions).  
 <img alt="FUP.drawio (1).png" src="/images/moengage_d86b66.png" />

# Fair Usage Policy Limits and Actions

The following tables showcase the FUP limits and actions taken if the limit is breached for a given end user.

## Non-SDK Sources (File Imports, Partner Integrations, Data APIs)

The limits below are cumulative across all non-SDK sources.

| Time Interval | Maximum Number of Data Requests Allowed in the Specified Time Interval | Action Taken If the Maximum Data Request Limit Is Breached |
| --- | --- | --- |
| 1 minute | 5,000 | Blocks the tracked data of that user for the next 1 hour. |
| 5 minutes | 10,000 | Blocks the tracked data of that user for the next 1 hour. |
| 1 hour | 50,000 | Blocks the tracked data of that user for the next 24 hours. |
| 1 day | 100,000 | Blocks the tracked data of that user for the next 24 hours. |

## SDK

| Time Interval | Maximum Number of Data Requests Allowed in the Specified Time Interval | Action Taken If the Maximum Data Request Limit Is Breached |
| --- | --- | --- |
| 1 minute | 500 | Blocks the tracked data of that user for the next 1 hour. |
| 5 minutes | 1,000 | Blocks the tracked data of that user for the next 1 hour. |
| 1 hour | 5,000 | Blocks the tracked data of that user for the next 24 hours. |
| 1 day | 10,000 | Blocks the tracked data of that user for the next 24 hours. |

<Warning>
For any of the above time intervals, the limit breach can occur a maximum of three consecutive times before MoEngage imposes a 7-day block. If imposed, all data tracking for that specific user is suspended for a 7-day duration.
</Warning>

## Fair Usage Policy on User Profile Activity Info

User profile activity info is powered by a high-speed storage for recent user actions. These events are stored for up to 30 days. To ensure optimal performance, if the number of events for a single user exceeds 12,000 or if the size exceeds 12MB, the older data is automatically purged. However, do note that there is a permanent storage (based on your retention policy) for all events, including those no longer visible in the activity info. These events are safe and accessible on segmentation and analytics.

## Fair Usage Policy on Number of Devices

A user profile can support a maximum of 1,000 devices. On breaching this limit, any subsequent devices, along with all associated user and event data, will not be processed in MoEngage.

## User and Device Data Expiry

A user or device record that receives no activity for 60 days is automatically removed from MoEngage. This applies only to dormant records with no incoming data in that window; events for active users continue to be stored per your account's Data Retention Policy.

# FAQ

<Accordion title="Is the maximum number of data requests allowed within a specified time interval configurable?">

No, it is not configurable.

</Accordion>
<Accordion title="Is the maximum number of data requests allowed within a specified time interval configurable?">

FUP is evaluated at the user level and not at the device level.

</Accordion>